console.log(15 && 17); // 15 is true so it returns 17, the second operand
console.log(0 && 'test') // 0 is falsy so it returns 0, the first operand
console.log("" || 'hello') // "" is falsy, it returns the second operand
console.log('truthy' || 0) // returns 'truthy' ß